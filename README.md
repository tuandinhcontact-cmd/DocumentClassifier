# DocumentClassifier
Build a document classification system based on machine learning, where textual data is preprocessed and transformed into vector representations using Word2Vec. Address class imbalance using SMOTE, optimize hyperparameters with GridSearchCV, and employ an ensemble model (Soft Voting) to combine predictions from base learners (LR, NB, SVM, etc.)
